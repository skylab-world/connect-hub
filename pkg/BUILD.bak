# pkg/BUILD

load("@io_bazel_rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "go_default_library",
    srcs = glob(
        ["**/*.go"],
        exclude = ["**/*_test.go"],
    ),  # Exclude test files from the library
    visibility = ["//visibility:public"],
)

go_test(
    name = "cloudops_tests",
    srcs = glob(["**/*_test.go"]),  # Automatically include all _test.go files in current and subdirectories
    embed = [":go_default_library"],
    importpath = "github.com/skylab-world/connect-hub/pkg",
)


# TODO remove .bak suffix from file name when GO files are added to this directory